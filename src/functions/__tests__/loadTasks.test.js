import loadTasks from "../loadTasks";

describe("loadTasks()", () => {
  test.todo("turns JSON data in localStorage to valid Task Objects");

  test.todo("logs error if no JSON data");

  test.todo("logs error if JSON data contains no task objects");

  test.todo("logs error if JSON data is malformed");
});
